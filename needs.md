# 股票分析系统产品设计需求文档

## 文档信息

|项目|内容|
|---|---|
|文档版本|V1.0|
|编写日期|2026-02-23|
|系统名称|智能股票板块异动分析系统|
|核心目标|基于财经新闻+实时行情，自动分析开盘后板块上涨及归因|
## 一、产品概述

### 1.1 产品定位

面向个人投资者/机构投研人员，提供**实时板块异动监控+新闻舆情分析+上涨原因自动归因**的智能化股票分析工具，核心依托 akshare 财经数据接口，通过定时拉取数据+多Agent智能分析，输出可读的板块分析报告。

### 1.2 核心价值

- 自动化：替代人工盯盘、新闻筛选，实时输出板块异动信息

- 精准化：基于数据匹配+逻辑推理，明确板块上涨核心原因

- 高效化：开盘后分钟级输出分析结果，支撑投资决策

### 1.3 产品边界

- 仅覆盖A股市场，聚焦行业/概念板块层面分析，不涉及个股深度分析

- 数据来源完全依托 akshare 公开接口，不接入非合规数据源

- 分析结果为辅助参考，不构成投资建议

## 二、核心功能需求

### 2.1 数据采集模块（非Agent，定时器驱动）

#### 2.1.1 功能目标

定时、自动化拉取 akshare 财经新闻、实时行情、板块基础数据，输出结构化数据并完成存储。

#### 2.1.2 详细需求

|数据类型|采集频率|采集内容（基于akshare接口）|数据格式|存储要求|
|---|---|---|---|---|
|财经新闻|开盘前：8:30 1次/日<br>盘中：每10分钟1次|东方财富财经新闻（标题、发布时间、内容、来源）、政策新闻（政策名称、发布主体、影响领域）|JSON|MySQL（持久化）+ 向量库（语义检索）|
|实时行情|盘中：9:30-15:00 每1分钟1次|行业/概念板块涨幅、成交额、量比、资金流向（流入/流出）、涨停家数|JSON|Redis（实时缓存）+ MySQL（历史归档）|
|板块基础数据|开盘前：8:00 1次/日|概念/行业板块成分股列表、板块行业分类、板块历史行情基准值|JSON|MySQL（持久化，每日更新）|
#### 2.1.3 异常处理

- 接口调用失败时，自动重试3次（间隔10秒），重试失败则记录日志并触发告警

- 数据格式异常时，过滤脏数据，保留有效字段并标记异常

### 2.2 新闻解析Agent（NewsAgent）

#### 2.2.1 功能目标

将采集的非结构化新闻转化为机器可识别的结构化事件，关联对应板块并打情绪标签。

#### 2.2.2 详细需求

1. **关键信息提取**：从新闻文本中提取核心要素（事件主体、事件类型、发布时间、影响领域）

2. **板块关联**：基于提取的关键词，匹配 akshare 板块基础数据中的行业/概念板块，输出“新闻-板块”映射关系

3. **情绪判定**：对新闻事件做“利好/利空/中性”标签判定，同时输出情绪强度（0-1分，分值越高影响越强）

4. **输出格式**：事件-板块-情绪强度三元组（JSON格式），包含字段：事件ID、新闻标题、事件类型、关联板块名、情绪标签、情绪强度、发布时间

### 2.3 板块监控Agent（SectorAgent）

#### 2.3.1 功能目标

实时监控板块行情，识别异动板块，过滤随机波动噪音。

#### 2.3.2 详细需求

1. **异动判定规则**：

    - 涨幅阈值：板块涨幅＞2%（可配置）

    - 成交额阈值：板块成交额＞5亿元（可配置）

    - 持续性：涨幅连续2分钟维持在阈值以上

2. **噪音过滤**：对比 akshare 板块历史行情（近7日），过滤单日随机波动（如涨幅超阈值但无资金流入）

3. **强度评分**：对异动板块输出强度评分（0-1分），评分维度：涨幅占比、资金流入规模、涨停家数

4. **输出格式**：异动板块列表（JSON格式），包含字段：板块名、实时涨幅、成交额、资金流向、强度评分、异动触发时间、是否为新异动

### 2.4 上涨归因Agent（ReasonAgent）

#### 2.4.1 功能目标

匹配异动板块与新闻事件，分析板块上涨核心原因，输出归因结果及置信度。

#### 2.4.2 详细需求

1. **归因匹配规则**：

    - 时间匹配：新闻发布时间与板块异动时间差＜30分钟

    - 主体匹配：新闻关联板块与异动板块完全一致

    - 资金验证：异动板块需有正向资金流入（基于 akshare 资金流向数据）

    - 交叉验证：多个新闻事件指向同一板块时，提升置信度

2. **归因类型**：明确标注上涨原因类型，包括：政策驱动、资金抱团、业绩驱动、题材发酵、外围市场传导

3. **置信度评分**：对归因结果输出0-1分置信度，评分规则：

    - 满足全部匹配规则：置信度≥0.8

    - 仅满足时间+主体匹配：置信度0.5-0.7

    - 无匹配新闻事件：置信度≤0.4（标注为“资金短期炒作”）

4. **输出格式**：归因结果（JSON格式），包含字段：板块名、异动时间、核心上涨原因、归因类型、关联新闻ID、置信度、资金流入验证结果

### 2.5 报告生成Agent（ReportAgent）

#### 2.5.1 功能目标

整合分析数据，生成人类可读的盘中快讯和盘后复盘报告。

#### 2.5.2 详细需求

1. **盘中快讯**（每10分钟输出1次）：

    - 核心内容：当前领涨板块（TOP5）、涨幅、核心上涨原因、关键催化新闻

    - 格式：简洁文本，适配移动端阅读

2. **盘后复盘报告**（15:30输出）：

    - 核心内容：当日整体板块行情、领涨/领跌板块对比、核心驱动事件汇总、板块上涨原因分类统计、次日风险提示

    - 格式：结构化报告（支持导出PDF/HTML），可关联 akshare 历史数据做趋势对比（如“今日新能源板块涨幅超近7日均值2个百分点”）

3. **输出要求**：语言通俗易懂，避免专业术语堆砌，核心信息突出。

### 2.6 辅助功能：风险过滤

#### 2.6.1 功能目标

过滤无效/错误数据，避免错误归因。

#### 2.6.2 详细需求

1. 过滤谣言/过期新闻：校验新闻来源可信度，过滤发布时间超24小时且无持续影响的新闻

2. 数据一致性校验：核对 akshare 接口返回数据的完整性，缺失关键字段（如资金流向）的板块不参与归因

3. 异常归因过滤：置信度＜0.4的归因结果不纳入最终报告

## 三、非功能需求

### 3.1 性能需求

- 数据采集延迟：≤5秒（从触发采集到数据存储完成）

- 板块异动识别延迟：≤1分钟（从行情更新到异动判定完成）

- 归因分析延迟：≤2分钟（从异动识别到归因结果输出）

- 系统并发：支持同时分析≥50个异动板块

### 3.2 可靠性需求

- 系统可用性：≥99.5%（A股交易时间内）

- 数据准确性：结构化数据解析准确率≥95%，归因结果准确率≥85%

- 故障恢复：接口调用失败后，自动恢复时间≤1分钟

### 3.3 可配置性需求

- 支持自定义配置：板块异动涨幅阈值、成交额阈值、归因时间匹配窗口

- 支持开关控制：开启/关闭特定板块的监控、开启/关闭特定类型的归因分析

### 3.4 安全性需求

- 数据存储：敏感配置（如接口密钥）加密存储，MySQL数据定期备份

- 日志管理：记录所有数据采集、Agent分析操作日志，日志保留≥30天

## 四、产品流程

### 4.1 开盘前流程（8:00-9:25）

1. 8:00：定时器触发，拉取板块基础数据（akshare）并更新至MySQL

2. 8:30：定时器触发，拉取财经新闻/政策新闻（akshare），NewsAgent完成新闻解析，生成事件-板块-情绪强度三元组

3. 9:00-9:25：系统预加载数据，完成初始化，等待开盘

### 4.2 盘中流程（9:30-15:00）

1. 定时器每分钟触发，拉取实时行情数据（akshare）并缓存至Redis

2. SectorAgent实时监控行情，识别异动板块并输出异动列表

3. ReasonAgent匹配异动列表与新闻事件，完成上涨归因并输出置信度

4. 风险过滤模块校验归因结果，过滤无效数据

5. ReportAgent每10分钟生成1次盘中快讯，输出至前端展示

### 4.3 盘后流程（15:00-15:30）

1. 15:00：定时器触发，拉取当日收盘行情数据（akshare）并归档至MySQL

2. 15:10：ReasonAgent完成当日所有异动板块的最终归因

3. 15:30：ReportAgent生成盘后复盘报告，支持导出

## 五、数据来源

核心数据完全依托 akshare 开源财经数据接口，具体接口清单：

1. 新闻类：`news_stock_em()`（东方财富财经新闻）、`news_policy_cn()`（政策新闻）

2. 行情类：`stock_board_industry_ths()`（行业板块实时涨幅）、`stock_zh_a_spot()`（个股实时行情）、`stock_board_fund_flow()`（板块资金流向）

3. 基础数据类：`stock_board_concept_name_ths()`（概念板块成分股）、`stock_industry_classify()`（行业分类）

## 六、输出物清单

1. 盘中快讯（文本/JSON）：每10分钟更新，包含领涨板块、上涨原因、核心新闻

2. 盘后复盘报告（PDF/HTML/TXT）：每日15:30生成，包含当日板块行情总结、归因分析、风险提示

3. 原始数据文件（JSON）：新闻数据、行情数据、Agent分析结果，支持下载

### 总结

1. 系统核心架构为「定时器+akshare数据采集」+「4个核心Agent分析」，数据层不设Agent，聚焦机械性数据拉取，Agent层聚焦智能分析；

2. 核心逻辑围绕“数据拉取→新闻解析→板块监控→归因分析→报告输出”展开，所有分析均基于akshare标准化数据；

3. 关键规则可配置（如异动阈值、归因时间窗口），输出物覆盖盘中实时分析和盘后复盘，满足用户对“新闻分析、板块上涨、上涨原因”的核心需求。
